# PSC Accounting API Environment Configuration

# Storage Mode: "local" or "database"
# local - stores PDF files on disk
# database - stores PDF files in PostgreSQL as base64
STORAGE_MODE=local

# Local storage directory (relative to backend folder)
UPLOAD_DIR=uploads

# Database configuration (if using database storage)
# These should match your existing database.py settings
DB_HOST=pscdb.cnacsqi4u8qw.eu-west-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=pscdb
DB_USER=postgres
DB_PASSWORD=Il1k3f1sh1ngperch!

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Development settings
DEBUG=true
